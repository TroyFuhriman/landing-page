{
  "name": "landing-page",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page with minified assets for production",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "clean-css": "^5.3.0",
    "terser": "^5.31.0"
  },
  "engines": {
    "node": ">=18"
  }
}
